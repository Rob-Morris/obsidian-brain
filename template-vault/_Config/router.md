Prefer `brain_read`/`brain_action` MCP tools if available.
Otherwise read [[.brain-core/index]].

Always:
- Every artefact belongs in a typed folder. Prefer existing types over new ones.

Conditional:
- After meaningful work → [[_Config/Taxonomy/Temporal/logs]]
- Before complex work → [[_Config/Taxonomy/Temporal/plans]]
- After conversation worth preserving → [[_Config/Taxonomy/Temporal/transcripts]]
- When a new idea strikes → [[_Config/Taxonomy/Temporal/idea-logs]]
