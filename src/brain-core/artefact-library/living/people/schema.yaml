required:
  type:
    const: "living/person"
  key:
    type: string
    pattern: "^(?=.{1,64}$)(?=.*[a-z])[a-z0-9]+(?:-[a-z0-9]+)*$"
  tags:
    type: array
    contains: "person/{key}"
optional:
  status:
    enum: [active, shaping, parked, deprecated]
    default: active
