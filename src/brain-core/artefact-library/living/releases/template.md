---
type: living/release
tags:
  - release
status: planned
version:
tag:
commit:
shipped:
---

{{agent: If you are authoring this artefact without `artefact.create` tooling, also set `key:` (canonical format: lowercase alphanumeric with hyphens, must include a letter; see .brain-core/standards/keys) and `parent:` as the owning living artefact's canonical key (for example `project/brain`), then file this artefact under `Releases/{scope}/` where `scope` is that key tokenised (`project~brain`) and add the matching `project/{owner-key}` relationship tag under `tags:`. Tooling injects these automatically. Delete this line once applied.}}

## Goal

## Acceptance Criteria

| Criterion | Status |
|---|---|
|  | pending |

## Designs In Scope

- 

## Release Notes

## Sources

- 
