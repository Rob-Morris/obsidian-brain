required:
  type:
    const: "living/documentation"
  key:
    type: string
    pattern: "^(?=.{1,64}$)(?=.*[a-z])[a-z0-9]+(?:-[a-z0-9]+)*$"
  tags:
    type: array
    contains: "documentation"
optional:
  status:
    enum: [new, shaping, ready, active, deprecated]
    default: active
