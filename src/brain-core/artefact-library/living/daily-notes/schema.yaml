required:
  type:
    const: "living/daily-note"
  key:
    type: string
    pattern: "^(?=.{1,64}$)(?=.*[a-z])[a-z0-9]+(?:-[a-z0-9]+)*$"
  tags:
    type: array
    contains: "daily-note"
  date:
    type: string
    pattern: "^\\d{4}-\\d{2}-\\d{2}$"
