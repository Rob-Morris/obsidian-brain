required:
  type:
    const: "living/workspace"
  tags:
    type: array
    contains: "workspace/{slug}"
optional:
  status:
    enum: [active, parked, completed]
    default: active
  workspace_mode:
    enum: [embedded, linked]
    default: embedded
