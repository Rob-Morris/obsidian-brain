---
type: living/wiki
tags:
  - topic-tag
---

{{agent: If authoring without `artefact.create`, set `key:` to a canonical lowercase alphanumeric-and-hyphen identifier containing at least one letter. For an owned page, also set `parent:` to the immediate owner's canonical `{type}/{key}`, mirror that value in `tags`, and derive the folder from the complete ownership chain. See .brain-core/standards/keys and .brain-core/standards/subfolders. Tooling handles these fields and placement automatically. Delete this line once applied.}}
