required:
  type:
    const: "living/writing"
  tags:
    type: array
    contains: "writing"
optional:
  status:
    enum: [draft, editing, review, published, parked]
    default: draft
  publisheddate:
    type: string
    pattern: "^\\d{4}-\\d{2}-\\d{2}$"
