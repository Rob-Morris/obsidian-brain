required:
  type:
    const: "living/journal"
  key:
    type: string
    pattern: "^[a-z0-9]+(-[a-z0-9]+)*$"
  tags:
    type: array
    contains: "journal/{key}"
optional:
  status:
    enum: [active, parked]
    default: active
