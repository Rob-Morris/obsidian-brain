required:
  type:
    const: "living/journal"
  key:
    type: string
    pattern: "^(?=.{1,64}$)(?=.*[a-z])[a-z0-9]+(?:-[a-z0-9]+)*$"
  tags:
    type: array
    contains: "journal/{key}"
optional:
  status:
    enum: [active, parked, deprecated]
    default: active
