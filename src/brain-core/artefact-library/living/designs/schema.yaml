required:
  type:
    const: "living/design"
  tags:
    type: array
    contains: "design"
optional:
  status:
    enum: [proposed, shaping, ready, active, implemented, deprecated, parked]
    default: shaping
