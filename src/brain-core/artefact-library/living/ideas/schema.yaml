required:
  type:
    const: "living/idea"
  key:
    type: string
    pattern: "^(?=.{1,64}$)(?=.*[a-z])[a-z0-9]+(?:-[a-z0-9]+)*$"
  tags:
    type: array
    contains: "idea"
optional:
  status:
    enum: [new, shaping, ready, adopted, deprecated, parked]
    default: new
