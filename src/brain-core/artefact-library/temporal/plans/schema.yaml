required:
  type:
    const: "temporal/plan"
  tags:
    type: array
    contains: "plan"
optional:
  status:
    enum: [draft, shaping, approved, implementing, completed, deprecated, parked]
    default: draft
