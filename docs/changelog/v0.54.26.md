# v0.54.26

**Migrate typed router repair and rebuild owners**

- Add distinct operator commands for idempotent `runtime.repair-router` and
  unconditional `runtime.rebuild-router` intent.
- Extract portable router-maintenance semantics so typed commands and the
  existing repair script share compilation, persistence, semantic-sidecar
  invalidation and session refresh ownership.
- Preserve no-op and dry-run planning for repair while making explicit rebuild
  produce a committed effect even when the prior cache is fresh.
- Report session-mirror failure as known partial application after router
  persistence, and retain unknown outcomes for earlier unclassified failures.
- Return bounded cache reason, removed sidecars and refresh state without vault
  or interpreter paths.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.26;
  CLI and proxy protocol versions remain unchanged.
