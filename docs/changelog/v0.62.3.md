# v0.62.3

**Summary:** Add isolated Brain Lab fixture clients

**Type:** Brain Core patch; Brain Lab host fixtures

## Changed

- Add `brain-lab fixture create` to expose retained runs through isolated Codex
  and Claude layouts without copying credentials or writing host client
  configuration.
- Make `_verify_active_brain` use `CONTAINER_PYTHON`, equivalent MCP
  registrations, before/after run manifests, and atomic no-replace fixture
  publication.
- Let `load_effective_skill_adapter` consume a validated `PackageSnapshot`,
  with an old-Core fallback in `active_brain_probe.py`.
