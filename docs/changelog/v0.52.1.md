# v0.52.1

**Correct MCP request guidance and missing-file error signalling**

- Document `brain_edit`'s required top-level `request` envelope in the functional and user references, and guard the generated wire shape with schema validation.
- Update shipped shaping, presentation, printable, wikilink, publishing, and archive guidance to use the current discriminated action, dedicated lifecycle, and move contracts.
- Return missing artefact, file, and archive reads as `CallToolResult(isError=True)`; make `read.py`/`brain read` failures use stderr and exit 1; and preserve valid markdown that begins with `Error:` across read, create, edit, shaping, and scan workflows.
- Align the Brain version badge and `brain` installer pin with v0.52.1; CLI dispatch behaviour and `BRAIN_CLI_VERSION` are unchanged.
