# v0.43.5

**Summary:** Compose brain doctor and harden machine-runtime safety gates

- Add `src/brain-core/scripts/doctor.py` as the composed `brain doctor` owner so the CLI can render CLI/PATH/Python basics, machine-level diagnosis from `doctor_machine.py`, and the current vault's own `check.py --json` as separate sections.
- Harden `src/brain-core/scripts/_machine/topology.py` live-process detection so spaced runtime paths and `python*` sibling spellings still block legacy-venv migration and orphan-runtime pruning when a process is active.
- Extend `tests/test_machine.py`, `tests/test_doctor.py`, `tests/test_brain_cli.py`, and `tests/test_managed_wrapper_handoff.py` to cover prune-side live-process guards, delegated repair failure branches, composed Doctor rendering, and the launcher-safe `compile_router.py` router-scope handoff used during upgrade validation.
