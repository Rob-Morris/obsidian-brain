# v0.55.7 — Safe Cutover and Bounded Reads

**Restore session.start cutover and bounded read-only execution**

**Date:** 2026-08-12
**Type:** Brain Core patch release

## Fixed

- Made `migrate_to_0_55_0.migrate()` update exact shipped `brain_session` instructions in root `AGENTS.md`/`Agents.md` and `CLAUDE.md` even without `.brain/config.yaml`, while completing fail-closed profile validation before any migration writes.
- Restored `brain_mcp.server._check_version_drift()` before MCP context composition and executor entry so `.brain-core/VERSION` changes reach proxy replacement through exit code 10 within one call.
- Replaced per-refresh `ThreadPoolExecutor` abandonment in `CapabilityRefresher` with a fixed process-wide daemon probe bound that respects provider and aggregate deadlines without delaying CLI exit or accumulating unbounded MCP workers.
- Made `FileReceiptStore.read()` return missing and expired outcomes without creating `.brain/local/command-outcomes`, acquiring a write lock or deleting retention state; writes and explicit `cleanup()` retain bounded maintenance.
- Added regression coverage in `test_profile_migration.py`, `test_granular_command_adapter.py`, `test_released_command_surface.py`, `test_availability.py` and `test_file_receipt_store.py` for the four restored contracts.
