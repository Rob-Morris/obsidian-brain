# v0.69.1

**Summary:** Preserve workspace identity and revalidate setup scaffold targets

**Type:** Brain Core patch; workspace regression fixes

## Changed

- Reject workspace hub type changes through document.update-frontmatter, including hubs with explicit self-membership.
- Bind workspace.setup Git resolution and scaffold destinations through the caller lock, preserving accurate committed effects.
- Exclude archived hubs from their own live membership lookup in vault.check while reporting stranded member references.
- Normalize stored filename-form parent references during artefact.set-workspace traversal and validation.
