# v0.61.2

**Summary:** Reject unsafe MCP proxy requests before dispatch

**Type:** Backward-compatible Brain Core patch; proxy ingress correctness

## Changed

- Validate JSON-RPC request identifiers before in-flight tracking so malformed frames cannot terminate the proxy.
- Reject request-only tools/call notification shapes before interface acceptance or child dispatch.
