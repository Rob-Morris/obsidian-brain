# v0.54.38

**Add receipt-safe agent-skill launcher configuration ownership**

- Add the typed `_launcher.agent_skill` owner for
  `agent-skill.configure`, with closed client/action intent and bounded
  per-client results.
- Carry the machine home directory only in trusted `LauncherContext`; the
  semantic request cannot inject an arbitrary client configuration root.
- Add genuine no-write planning to the shared ownership-safe client adapter
  seam, including replacement conflicts, adoption, update, removal and the
  exact backup destination.
- Preflight every requested client before the first write so an existing
  conflict cannot leave an avoidable cross-client partial change. Unexpected
  races or I/O failures after apply begins remain non-retryable and
  outcome-unknown.
- Report committed client-skill and replacement-backup effects independently,
  while preserving the existing public `configure.py agent-skills` output and
  behaviour until the coordinated CLI 2.0 cutover.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.38;
  CLI and proxy protocol versions remain unchanged.
