# v0.62.6

**Summary:** Reconcile MCP registrations during brain.upgrade

**Type:** Brain Core and CLI patch; upgrade repair fix

## Changed

- Run the canonical `repair.py mcp` scope after `upgrade.py` provisions the target managed runtime, while leaving vaults without existing project registrations untouched.
- Project `mcp_registration_repair` through `BrainUpgradePayload` reconciliation so failed repair returns a recoverable partial upgrade instead of false success.
- Exercise the real v0.54.0-to-current path in `historical_upgrade_acceptance.py`, including migrations, runtime readiness, MCP startup, diagnostics, and cleanup.
