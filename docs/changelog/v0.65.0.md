# v0.65.0

**Summary:** Bound command discovery and expose concise access-aware listings

**Type:** Breaking discovery contract

## Changed

- Make command.list v3 concise by default, retain an explicit detailed view and bound canonical pages to 16,000 UTF-8 bytes with continuation.
- Show effective active or inactive access independently from dependency availability and improve task-oriented command descriptions.
- Make the CLI honour discovery request JSON and continuation cursors; emit shared catalogue provenance once in the v2 composed listing.
