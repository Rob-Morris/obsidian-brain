# v0.62.5

**Summary:** Make brain.upgrade readiness-aware and surface runtime cleanup

**Type:** Brain Core and CLI patch; upgrade completion fix

## Changed

- Make `brain.upgrade` await the canonical `runtime.status` ready state before returning full success, and report `brain runtime remove-orphans --dry-run` plus explicit removal only when read-only topology inspection proves candidates.
