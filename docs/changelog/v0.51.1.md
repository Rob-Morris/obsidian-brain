# v0.51.1

**Keep `create_artefact` keys unique across reused router snapshots**

- Update `create_artefact()` to add each successfully written living artefact to the supplied router's in-memory `artefact_index`, including direct-parent child counts, so sequential callers reserve generated and explicit keys without recompiling between creates.
- Add `test_reused_router_reserves_created_living_keys` coverage using clearly labelled test-design titles, distinct generated keys, complete in-memory entries, and a successful `compile_router.compile()` rebuild.
- Align the `README.md` version badge and `cli/brain` install pin with `src/brain-core/VERSION`; `BRAIN_CLI_VERSION` is unchanged because CLI dispatch behaviour did not change.
