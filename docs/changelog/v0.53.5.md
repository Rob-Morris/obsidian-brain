# v0.53.5

**Replace legacy review skills with `superpowers-brain` workflows**

- Remove the shipped `code-review` and `swarm-test` skill families from
  `src/brain-core/skills/`, leaving native Claude Code and Codex review
  workflows to handle ordinary reviews.
- Add the Brain-adapted `superpowers-brain` skill family, including its
  grouped planning, execution, debugging, review, verification, and
  subagent-development workflows.
- Update `docs/functional/config.md` to describe the revised core-skill set.
- Align `src/brain-core/VERSION`, `cli/brain`'s `BRAIN_INSTALL_REF`, and the
  repository README badge with v0.53.5.
