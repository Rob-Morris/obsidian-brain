# v0.43.6

**Summary:** Preserve structured semantic repair failures during upgrade

- Teach `src/brain-core/scripts/upgrade.py` to salvage a trailing JSON result from noisy `repair.py --json` output, so upgrade-time semantic repair failures keep their structured payload even when model/runtime dependencies emit prefix chatter.
- Preserve `returncode`, `stdout`, and `stderr` on post-upgrade retrieval-repair failures instead of collapsing everything into one opaque message.
- Extend `tests/test_upgrade.py` with a regression test for noisy `repair.py --json` failure output so a retriable semantic repair cannot lose the real cause again.
