# v0.69.0

**Summary:** Bind artefact mutations to canonical workspace context

**Type:** Breaking Brain Core minor; workspace-scoped mutation contracts

## Changed

- Give workspace-scoped artefacts durable membership plus shared and caller-local mutation defaults.
- Add typed workspace registration, policy, reassignment, lifecycle guards, and validation commands.
- Fail closed on stale or cross-workspace configuration while preserving historical terminal membership.
- Route shaping and supported command projections through one workspace-aware semantic mutation boundary.
