# v0.70.8

**Summary:** Preserve calendar dates and preflight document writes

**Type:** Patch; calendar date and document mutation correctness

## Changed

- Keep date-only inputs as calendar dates in parse_date_value and reconcile_timestamps, including historically skipped local days; retain local conversion for actual timestamps.
- Preflight document lifecycle moves and naming collisions through plan_document_lifecycle before admission, returning conflict with no effects for known collisions while retaining unknown outcomes after possible writes.
- Reuse transition_binding for prepared document authorisation so lifecycle destinations, backlink inputs and effects, and a frozen effective time are covered; reject changed collateral effects before execution.
- Add timezone regressions in tests/test_calendar_dates.py and creation, append, collision, rename, backlink and prepared-authorisation coverage in tests/application/; document the contracts in src/brain-core/standards/naming-conventions.md and docs/functional/mcp-tools.md.
