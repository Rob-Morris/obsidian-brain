# v0.62.20

**Summary:** Make Brain/CLI cutover rollback complete and truthful

**Type:** Brain Core patch; CLI patch

## Changed

- Record replacement intent before filesystem effects in `apply_file_changes`, `_write_transaction` and `install_distribution`, reconcile interrupted rollback from observed state, preserve complete migration recovery material through `SnapshotRestoreReport`, and expose committed CLI cleanup paths through `RecoveryRequiredDetails` plus the standalone `_commit_cli_cutover` result.
