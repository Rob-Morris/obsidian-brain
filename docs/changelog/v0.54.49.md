# v0.54.49

**Summary:** Add the staged granular MCP projection

- Register each MCP-eligible selected-Brain command mechanically as one
  `brain_<noun>_<verb>` FastMCP tool derived from the authoritative application
  catalogue.
- Expose the exact flat canonical request schema, including nested field
  descriptions, without a generic `request` envelope or caller-supplied command
  identity.
- Preserve omitted fields across FastMCP validation so the shared request
  resolver remains the sole owner of application defaults.
- Return the shared structural result as MCP structured content, concise text
  content and the correct MCP error state.
- Correct dependency-tier projection to the documented string vocabulary:
  `bootstrap`, `portable` and `managed`.
- Exercise every minimal canonical request through real FastMCP validation with
  authority denied before execution, and verify successful and denied calls
  separately.
- Keep granular registration staged and undispatched until the coordinated
  breaking cutover migrates public profiles and removes aggregate tools.
