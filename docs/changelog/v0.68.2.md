# v0.68.2

**Summary:** Refresh idle MCP servers and expose loaded runtime versions

**Type:** Backward-compatible Brain Core patch; MCP lifecycle controls

## Changed

- Add compact proxy-owned status and idle server refresh controls that remain available without a healthy child.
- Refresh installed compatible Core code before admitting the next command; retain the previous child on failed validation and leave in-flight work running.
- Expose proxy drift in model-visible JSON and correct stale replay guidance; dispatched commands are never replayed.
