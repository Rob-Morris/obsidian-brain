# v0.70.9

**Summary:** Clean promotion remnants with explicit ownership and safe retries

**Type:** Patch; contributor promotion housekeeping

## Changed

- Add preview-first promotion cleanup for validated finished local candidates, published remote refs and stale trackers; preserve unfinished candidates, dirty or foreign worktrees and recovery records.
- Require a full expected SHA to discard a remote-only candidate, including from narrow clones, and preserve same-name replacements and candidates already recorded on the ledger.
- Use exact-SHA local deletion and atomic leased remote cleanup with observed outcomes, so interrupted or partially completed cleanup can be retried without undoing publication.
- Document the cleanup lifecycle, recovery boundaries and Make wrappers, with disposable Git regression coverage for ownership, checkout interference and uncertain outcomes.
