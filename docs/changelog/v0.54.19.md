# v0.54.19

**Migrate typed named document mutation owners**

- Add 20 distinct edit, append, prepend, delete-section and replace-text
  commands across memory, skill, style and template domains.
- Keep every command in its domain/verb module with a concrete request type,
  while inherited typed field contracts prevent validation drift.
- Reuse the v0.54.18 structural selector, scope, inline/staged content, result
  and outcome seam without recreating a cross-resource aggregate request.
- Preserve each resource's existing config-path resolution, frontmatter merge
  mode, body mutation and write behaviour through `edit.py`.
- Keep `fix_links` artefact-only and reject caller-file content or fields that
  belong to another verb before mutation.
- Cover every new owner against disposable installed resources, plus staged
  content, frontmatter merging, missing resources and post-commit uncertainty.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.19;
  CLI and proxy protocol versions remain unchanged.
