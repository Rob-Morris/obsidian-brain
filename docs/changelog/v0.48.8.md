# v0.48.8

**Surface derived-cache repair guidance in check and Doctor**

**Derived cache diagnostics**
- Add `_lifecycle/derived_cache_state.py` as the shared dependency-light owner for compiled-router and lexical-index freshness inspection.
- Route `repair.py router` and `repair.py lexical` through that shared detector so repair dry-runs and mutations use the same cache-health rules that diagnostics consume.
- Extend `check.py` to report repairable stale router state with `repair.py router` guidance and missing/stale lexical retrieval index state with `repair.py lexical` guidance. Missing or unreadable router state still short-circuits structural checks; loadable stale router state is an error; lexical-index drift is warning-level.
- Keep semantic-enabled drift on the existing semantic owner: when semantic repair guidance is already present, `check.py` does not add competing lexical-only guidance. Composed `brain doctor` inherits the new current-vault findings through its existing `check.py --json` section.

**Regression coverage**
- Add `tests/test_check.py`, `tests/test_repair.py`, and `tests/test_brain_cli.py` coverage for the shared detector path, router stale/error guidance, lexical warning guidance, semantic suppression of lexical guidance, Doctor composition, and fresh lexical-index clean runs.
- Move `BRAIN_INSTALL_REF` to `v0.48.8`; `BRAIN_CLI_VERSION` is unchanged because the CLI dispatch surface did not change.
