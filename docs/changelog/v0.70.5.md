# v0.70.5

**Summary:** Preserve equivalent TOML transport and reap proxy preflight children

**Type:** Patch; MCP transport repair and proxy lifecycle

## Changed

- Treat equivalent TOML transport values as unchanged, preserving native formatting and approval settings without false stale-registration findings.
- Cover approval configuration followed by registration diagnostics and repair, while retaining detection and repair of genuine transport drift.
- Reap accepted and rejected proxy preflight children before exiting, closing a macOS process-group cleanup race without suppressing permission errors.
- Preserve privacy-safe lifecycle preparation diagnostics across proxy replacement: exception class, errno, proxy version and source line, without exception messages or private paths.
