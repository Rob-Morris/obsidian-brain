# v0.50.2

**Preserve strict migrations while accepting legacy no-op repair**

- Normalise the `v0.48.2` MCP project-registration migration so its upgrade-runner entry point returns `skipped` when every repair step is a no-op. Step-level diagnostics still say `noop`, preserving the operator-facing explanation.
- Keep the `v0.50.1` strict migration-runner policy intact: only top-level `ok` and `skipped` results are non-fatal, and blocked/error-like statuses still halt without ledger recording.
- Add forced-upgrade coverage proving the legacy no-op repair migration records as `skipped` instead of rolling back a same-version reapply.
- Align `cli/brain` install pin to `v0.50.2`; `BRAIN_CLI_VERSION` is unchanged because the shell dispatch surface did not change.
