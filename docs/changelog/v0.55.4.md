# v0.55.4 — Native Windows CLI Python Probe

**Correct native Windows CLI Python probing**

**Date:** 2026-08-11
**Type:** Brain Core patch release; coordinated CLI 2.0.3 patch

## Fixed

- Corrected the quoted Python-version expressions in `cli/brain.cmd` so `cmd.exe` passes the comparison operator to Python instead of preserving a caret that makes every candidate probe fail.
- Restored installed native Windows CLI startup when a compatible Python 3.12-or-newer interpreter is available.
- Added a static bootloader regression guard alongside the native Windows smoke coverage.
