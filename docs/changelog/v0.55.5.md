# v0.55.5 — Vault-Self MCP Project Context

**Accept vault-self project anchors in MCP context**

**Date:** 2026-08-11
**Type:** Brain Core patch release

## Fixed

- Restored direct and MCP workspace resolution after the context adapter retained a stale `resolve_startup_target` import instead of the canonical `resolve_brain_target` owner.
- Recognised a selected Brain vault root as a valid project-scoped caller anchor when startup resolution identifies it as `vault_self`.
- Preserved the vault root as the caller workspace for provider and command semantics without requiring a redundant workspace manifest inside the vault.
- Kept mismatched, stale and unbound workspace anchors fail-closed.
