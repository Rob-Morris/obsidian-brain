# v0.43.3 — Surface per-Brain MCP and registry drift guidance in brain doctor

- Split launcher-safe drift collection in `src/brain-core/scripts/_bootstrap/diagnostics.py` into `collect_registry_check_findings()`, `collect_runtime_check_findings()`, and `collect_mcp_check_findings()`, so `_machine/maintenance.py` can reuse only the brain-level MCP/workspace checks it actually needs.
- Tighten `src/brain-core/scripts/_machine/maintenance.py` and `src/brain-core/scripts/doctor_machine.py` so `brain doctor` reports shared-runtime topology from `_machine/` and per-Brain MCP / workspace drift as exact `repair.py mcp` / `repair.py registry` guidance, without duplicating runtime-failure findings.
- Rephrase the launcher-safe MCP finding in `src/brain-core/scripts/_bootstrap/diagnostics.py` from current-vault wording to Brain-level wording, matching the machine-level doctor output when a discovered Brain is not the current vault.
- Extend `tests/test_machine.py` and `tests/test_brain_cli.py` to cover the drifted-vault fixture, machine-doctor human + JSON repair guidance, healthy-path zero-drift assertions, and the real helper-vault dependency bundle used by `brain doctor`.
- Update `docs/functional/cli.md`, `docs/functional/scripts.md`, and `src/brain-core/scripts/README.md` so the shipped docs describe the new per-Brain drift guidance and the boundary between machine-owned runtime topology and Brain-owned MCP/workspace repair.
