# v0.57.1

**Summary:** Accept SDK 2 mapping metadata for proxy-owned MCP invocation IDs

**Type:** Brain Core patch; CLI install reference alignment

## Fixed

- Teach `server._invocation_id_from_metadata()` to accept the official SDK 2 `RequestParamsMeta` mapping while retaining the older Pydantic `model_extra` adapter shape. Proxy-injected `brainInvocation.invocationId` now composes trusted context on native Windows and every other supported host.
- Update `test_native_windows_install_and_granular_mcp_round_trip` to request a one-use `attachment.upload` lease before mutation, matching the Reader-default active grant introduced in v0.57.0, and retain direct/proxy diagnostics for future native-only failures.

## Verification

- `tests/mcp_server/test_released_command_surface.py` covers both supported metadata representations.
- GitHub Actions Windows user smoke exercises native installation, SDK 2 proxy/server invocation, exact elevation and attachment mutation.
