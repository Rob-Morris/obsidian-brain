# v0.42.4 — Single-source mcp_state.py and semantic_repairs.py ownership

- Add `src/brain-core/scripts/_bootstrap/mcp_state.py` as the launcher-safe owner for shared Claude/Codex MCP config layout, SessionStart hook command building, and init-state helpers used by both `src/brain-core/scripts/init.py` and `src/brain-core/scripts/_bootstrap/diagnostics.py`.
- Move managed semantic inspect/repair/check ownership into `src/brain-core/scripts/_lifecycle/semantic_repairs.py`, and repoint `src/brain-core/scripts/check.py` plus `repair.py semantic` through that canonical module instead of keeping semantic ownership in `src/brain-core/scripts/_repair_runtime.py`.
- Retire the old semantic compatibility seams from `src/brain-core/scripts/_repair_runtime.py`, and migrate `tests/test_init.py` and `tests/test_repair.py` to validate the `_bootstrap` and `_lifecycle` owners directly.
- Realign runtime-plane docs in `docs/functional/scripts.md`, `docs/architecture/overview.md`, `docs/contributor/agents.md`, and `src/brain-core/scripts/README.md` so the shipped and repo-facing module maps describe `_bootstrap/` and `_lifecycle/semantic_repairs.py` consistently.
