# v0.54.51

**Summary:** Enforce real-client token budgets for the granular MCP projection

- Capture the staged 109-tool projection through pinned Claude Code 2.1.226
  and Codex CLI 0.147.0 clients using localhost-only fake model endpoints and
  the real stdio FastMCP server.
- Prove a successful minimal `brain_command_list` request through each client,
  including Claude's eager declaration path and Codex's deferred tool-search
  path, without an external model request.
- Pin `tiktoken` 0.12.0 with `o200k_base` for deterministic release budgets;
  keep each projected tool below 512 tokens and both complete client
  catalogues below 16,384 tokens.
- Compact equivalent distinct-type JSON Schema unions and concise mechanical
  field labels while preserving strict schemas and descriptions throughout.
- Derive standard MCP read-only, destructive, idempotent and local-world hints
  from authoritative command effect and retry metadata.
