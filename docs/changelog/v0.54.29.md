# v0.54.29

**Migrate typed shaping-session start ownership**

- Add the granular contributor command `shaping.start` with an explicit
  `brainstorm`, `refine`, or `discover` mode.
- Reuse the portable shaping-session semantic owner for target resolution,
  lifecycle transition, same-day transcript creation/append and backlinking.
- Return typed target/transcript paths, path-transition state, transcript
  operation and the complete changed-path set.
- Require a fresh compiled router and preserve strict no-effect validation,
  mutation-lock conflict and known-partial result boundaries.
- Reject legacy title and implicit-mode fields from the canonical request while
  leaving existing public adapters unchanged until coordinated cutover.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.29;
  CLI and proxy protocol versions remain unchanged.
