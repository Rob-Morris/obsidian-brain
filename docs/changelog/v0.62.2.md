# v0.62.2

**Summary:** Compose portable shaping with Brain capabilities

**Type:** Brain Core patch; shaping skill integration

## Changed

- Add `src/scripts/vendor_shaping_skill.py` and `portable-provenance.json` to
  vendor the portable shaping workflow from verified clean Git blobs with
  pinned provenance and no runtime source dependency.
- Compose the portable workflow through
  `src/brain-core/skills/shaping/references/brain.md`, planning target,
  decision/work, transcript, and lifecycle persistence independently before
  calling `shaping.start`, `artefact.set-status`, or other Brain mutations,
  including an authority preflight before create-and-refresh sequences.
- Migrate the exact previously shipped shaping-transcript router default to the
  confirmed-plan condition without overwriting customised router entries.
- Preserve the active-Brain client loader contract and cover fidelity,
  source-mismatch, override, and conditional transcript-trigger behaviour in
  `tests/test_shaping_skill_contract.py` and
  `tests/test_core_skill_portability.py`.
