# v0.49.5

**Correct tool count and script-usage drift in shipped Brain docs**

- Fix `src/brain-core/guide.md` Tooling section: the MCP server exposes ten tools (`brain_init` … `brain_process`, which it already lists), not nine.
- Align `src/brain-core/scripts/README.md` usage strings with the scripts' real flags: `read.py --vault`, `create.py --parent/--vault`, `edit.py --temp-path/--vault`, `search_index.py --tag/--status/--top-k`, and `shape_printable.py --keep-heading-with-next`.
- Move `cli/brain` `BRAIN_INSTALL_REF` to `v0.49.5`; `BRAIN_CLI_VERSION` is unchanged because the CLI dispatch surface did not change.
