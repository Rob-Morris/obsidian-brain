# v0.54.56

**Summary:** Stage one-time granular profile allow-list migration

- Add a pure cutover migration that never acts as a runtime aggregate fallback
  and returns no partial output on unknown authority.
- Replace exact historical built-ins with catalogue-derived granular sets of 41
  reader, 82 contributor and 109 operator leaves.
- Expand custom profiles only from the legacy capabilities they explicitly
  allowed, preserving unrelated profile metadata.
- Give custom mutating profiles `invocation.read` only when receipt-backed
  outcome recovery is required by a mapped command.
- Accept mixed legacy and already-granular input idempotently and fail closed on
  unknown tools or malformed profile definitions.
- Check the shipped migration map against the closed operation-disposition
  evidence so interface and upgrade mappings cannot drift independently.
