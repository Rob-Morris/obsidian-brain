# v0.48.9

**Round out repair.py dry-runs and vault repair boundaries**

**Repair consistency**
- Attach `_repair_common.attach_repair_guidance()` metadata to `_bootstrap/diagnostics.py` `mcp_legacy_vault_root` findings so legacy `BRAIN_VAULT_ROOT` diagnostics expose the same structured `repair.py mcp` hint as peer MCP findings.
- Extend `_repair_runtime.py` `repair_router(..., dry_run=True)` output to name the semantic embeddings sidecar clear that happens after a router rebuild.
- Drive `_lifecycle/semantic_repairs.py` dry-run marker planning from the actual semantic runtime marker state instead of always planning `semantic_runtime_marker`.
- Extract `_lifecycle/frontmatter_repairs.py` `detect_duplicate_frontmatter_documents()` as the repair-side duplicate-frontmatter preflight, while `check.py` keeps its `CheckContext`-cached diagnostic walk over the same `_common.inspect_duplicate_frontmatter_document()` rule.

**Repair boundaries**
- Document the vault-local versus machine-level repair split in `docs/architecture/overview.md`, `docs/functional/scripts.md`, `repair.py`, `_repair_runtime.py`, and `vault_registry.py`.
- Add `tests/test_doctor_machine.py`, `tests/test_repair.py`, and `tests/test_check.py` regression coverage for the new guidance, dry-run preview, semantic marker planning, and shared frontmatter preflight.
- Move `cli/brain` `BRAIN_INSTALL_REF` to `v0.48.9`; `BRAIN_CLI_VERSION` is unchanged because the CLI dispatch surface did not change.
