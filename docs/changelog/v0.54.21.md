# v0.54.21

**Migrate typed artefact transition owners**

- Add distinct `artefact.rename`, `artefact.convert`, `artefact.archive`,
  `artefact.unarchive` and `artefact.delete` application commands with
  operation-specific typed requests and structural results.
- Assign the destructive family operator authority, receipt-required retry and
  selected-Brain mutation effects while rejecting invented dry-run outcomes.
- Move same-type rename and archive-boundary validation into `rename.py`, so
  adapters no longer need to own the policy that separates rename from convert.
- Preserve the existing conversion, archive, restore, recursive-descendant,
  attachment-scope and wikilink semantics behind the application boundary.
- Represent known partial application separately from unknown post-entry
  failure, including compact committed effects and follow-up warnings for
  orphaned attachment scopes or uninspected archive candidates.
- Return the exact deleted path set from the semantic delete owner and verify
  durable moves, metadata, links, invalid intent and transport separation in
  disposable installed Brain vaults.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.21;
  CLI and proxy protocol versions remain unchanged.
