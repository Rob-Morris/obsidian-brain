# v0.42.0 — Restore portable artefact surfaces and remove compliance from brain_read

- Remove `compliance` from `read.py`, `brain_read(...)`, and the shared read-resource contract so live compliance execution stays on `check.py` instead of being smuggled through a fetch-style surface.
- Extract the shared broken-wikilink scan into `src/brain-core/scripts/_portable/links.py`, letting `fix_links.py` and the full compliance checker share one launcher-safe seam without `fix_links.py` importing `check.py`.
- Restore `list_artefacts.py` as a direct public script surface aligned to the `brain_list` resource/filter contract, including artefact, workspace, archive, and non-artefact collection listing.
- Pin the portable boundary with direct tests for the removed compliance read surface, the restored `list_artefacts.py` CLI, the `_portable.links` seam, and `start_shaping.py` staying on portable/shared imports only.
