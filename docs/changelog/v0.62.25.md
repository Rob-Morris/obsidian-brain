# v0.62.25

**Summary:** Preserve recovery evidence across command-interface boundaries

**Type:** Brain Core and CLI patch

## Changed

- Carry incomplete-upgrade recovery paths through Core results, launcher errors and receipts, and human output without masking the initiating CLI installation failure.
- Return caller-owned copies of stable derived-state snapshots so ordinary and base-method mutation cannot alter cached state or break asynchronous session mirroring.
- Release each temporary Git skill package stage when its inspection scope ends and share the Windows drive-prefix rule from the portable-path owner.
