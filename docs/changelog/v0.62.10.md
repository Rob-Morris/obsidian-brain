# v0.62.10

**Summary:** Make brain.upgrade rollback cover every declared effect

**Type:** Brain Core patch; CLI reliability

## Changed

- Require migration prospective_effects declarations for files outside the standard .brain, _Config and artefact-root snapshots, and restore each declared file before reporting rollback_verified.
- Treat verified CLI installation as committed before backup cleanup; surface cleanup_recovery_paths instead of rolling Brain Core back to a version older than the installed CLI.
- Rollback apply_file_changes, install_distribution, release._write_transaction and coordinated brain.upgrade effects on KeyboardInterrupt or SystemExit before re-raising the interruption.
- Cover declared migration effects, CLI cleanup failure and interruption boundaries in tests/test_upgrade.py, tests/application/test_cli_distribution.py, tests/application/test_launcher_mcp_owners.py and tests/test_release_tooling.py.
