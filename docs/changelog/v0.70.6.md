# v0.70.6

**Summary:** Keep approval journal ownership separate during CLI cutover

**Type:** Patch; managed approval upgrade fix

## Changed

- Fix CLI installation and Brain upgrades with managed approvals enabled: the transaction no longer treats creation of its own recovery journal as an external configuration edit.
- Preserve pending-journal recovery requirements and concurrent native configuration protections; cover real distribution replacement and nested upgrade transitions.
- Make concurrent consent-preparation coverage deterministic: explicitly verify the existing conflict-and-retry contract and preservation of the winner's retained input, without changing consent behaviour.
