# v0.54.37

**Add effect-free Doctor and operator-key launcher owners**

- Add distinct `_launcher.doctor` and `_launcher.operator` owners for
  `brain.doctor` and `operator.generate-key`, preserving their reader/operator
  authority and safely retryable no-effect contracts.
- Return bounded `BrainDoctorPayload` topology, registry, runtime and optional
  vault findings; translate repair scopes to canonical command identifiers and
  exclude embedded shell commands from the typed result.
- Add `inspect_machine_registry` and the `synchronise_registry=False` summary
  path so canonical Doctor diagnosis reports `brains.json` drift without
  creating locks, backups or registry writes. The current public adapter stays
  unchanged until the coordinated CLI 2.0 cutover.
- Add `generate_key_material` as the reusable key/digest seam beneath the
  existing `generate_key.py` output and bound typed requests to 1–20 candidates.
- Carry trusted `cli_binary` identity in `LauncherContext` so Doctor can report
  CLI/PATH state without accepting adapter facts as caller-controlled intent.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.37;
  CLI and proxy protocol versions remain unchanged.
