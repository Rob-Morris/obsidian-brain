# v0.49.3

**Unify the heading intro boundary and correct its scope contract**

- Collapse the redundant per-heading `intro_end` loop in `_scan_structural_nodes` and unify it with the document-root intro on a shared `_next_heading_start` helper; resolver behaviour is unchanged.
- Correct the heading `scope="intro"` contract descriptions (`_SCOPE_MEANINGS` and `brain_edit_scope_description` in `edit.py`) to state that a childless heading's intro spans its whole body.
- Add `tests/test_markdown_structural.py::TestResolveStructuralTarget::test_heading_intro_for_last_heading_runs_to_eof` pinning the last-heading end-of-file branch.
- Move `cli/brain` `BRAIN_INSTALL_REF` to `v0.49.3`; `BRAIN_CLI_VERSION` is unchanged because the CLI dispatch surface did not change.
