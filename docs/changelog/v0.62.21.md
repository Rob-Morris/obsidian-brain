# v0.62.21

**Summary:** Keep long-lived MCP state bounded and current

**Type:** Brain Core patch

## Changed

- Retain baseline and explicitly refreshed capability snapshots across bounded `command.list` pagination while config, workspace and dependency-tier inputs remain current.
- Drain the newest accepted session mirror on shutdown without evicting it, and report when the fixed close deadline expires.
- Publish derived router and lexical snapshots only after a bounded stable-signature load, and make cached values recursively read-only to prevent caller mutation without per-call copies.
