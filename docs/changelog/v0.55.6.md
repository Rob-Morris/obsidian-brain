# v0.55.6 — Cache-Independent Runtime Environment

**Make runtime environment reads cache-independent**

**Date:** 2026-08-11
**Type:** Brain Core patch release

## Fixed

- Made `runtime.read-environment` report live bounded vault, platform, Python and CLI facts without requiring `.brain/local/compiled-router.json`.
- Moved runtime detection into the portable lower owner shared by command execution and router compilation, avoiding duplicated fact semantics.
- Preserved compiled-router environment snapshots for session and router consumers while allowing fresh-install diagnostics to run before derived caches exist.
