# v0.43.4

**Summary:** Add brain machine migration and orphan-runtime pruning

- Add `brain machine` as the launcher-safe machine-level maintenance surface beneath the CLI family.
- Ship `migrate-legacy` to delegate per-Brain `repair.py` runtime / MCP / registry convergence, remove legacy vault-local `.venv` directories only after clean delegated repair, and verify central-runtime migration afterward.
- Ship `prune-runtimes` to remove only shared central runtimes already proven orphaned by the canonical machine registry plus live-process detection, with structured per-target error reporting and `--dry-run` support.
- Route the new surface through the same source-Brain bootstrap used by `brain doctor`, and document the machine/Brain ownership boundary across the CLI, scripts reference, and user tooling guide.
