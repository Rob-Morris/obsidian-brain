# v0.62.9

**Summary:** Use the authorised document mutation names

**Type:** Brain Core patch; interface conformance

## Changed

- Rename the three document mutation commands to document.write-body, document.replace-text and document.structured-edit across MCP, CLI, direct-script and typed-Python projections, without runtime aliases.
- Migrate v0.62.8 profile grants one-to-one through migrate_to_0_62_9, preserving custom authority and failing before writes on unknown grants.
- Recapture real Claude Code 2.1.251 and Codex CLI 0.150.1 stdio calls and derive deterministic projection pins from the observed client evidence.
- Make release preparation refresh the derived command-catalogue route so catalogue changes cannot leave a stale session fingerprint.
