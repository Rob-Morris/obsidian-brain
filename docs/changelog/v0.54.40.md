# v0.54.40

**Add receipt-safe legacy Brain migration launcher ownership**

- Add the typed `_launcher.machine` owner for `machine.migrate-legacy`, with an
  optional canonical Brain ID or absolute-path selector and bounded target/step
  results.
- Carry the current Brain only as trusted launcher context and compare derived
  machine state without rewriting `brains.json` as an implicit migration
  effect.
- Reuse the existing version-matched runtime, MCP and registry repair processes,
  followed by live-process-safe removal of the legacy vault-local runtime.
- Distinguish no-effect child spawn failure, known child partial outcomes and
  non-retryable unknown outcomes caused by child timeout, invalid output or
  recursive-deletion failure.
- Report changed or partially changed repair scopes and removed legacy runtime
  directories as committed effects, with a genuine no-write dry-run plan.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.40; CLI
  and proxy protocol versions remain unchanged.
