# v0.62.12

**Summary:** Restore long-lived MCP state and non-blocking session mirrors

**Type:** Brain Core patch

## Changed

- Retain authenticated identity plus signature-refreshed router and lexical snapshots across MCP calls, with explicit rebuild-command invalidation.
- Publish MCP session mirrors through one bounded coalescing worker while direct CLI/script calls remain synchronous, and skip byte-identical writes.
- Cover config and registry refresh, derived snapshot reuse/invalidation, coalescing persistence, and identical-mirror suppression with focused tests.
