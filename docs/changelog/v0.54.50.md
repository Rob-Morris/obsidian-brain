# v0.54.50

**Summary:** Stage the bounded session command-discovery route

- Ship a static catalogue route containing only schema, interface epoch,
  authoritative fingerprint and installed application-command count.
- Verify the static route against the application catalogue so it remains a
  derived cache rather than a second semantic authority.
- Add the route to typed `session.start` v2 with concise directions to
  `brain_command_list` and `brain_command_describe`; keep legacy
  `brain_session` output unchanged until coordinated cutover.
- Keep detailed commands, request schemas and dynamic availability out of the
  bootstrap route.
- Enforce the 512-byte route budget and checked 30-cold-process/200-warm-sample
  latency gates without application imports or provider probes.
