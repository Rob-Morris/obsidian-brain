# v0.35.0 — 2026-05-02

**Split MCP startup with `brain_init` and background warmup.** The MCP server now answers `initialize` from a minimal runtime skeleton, exposes additive `brain_init` bootstrap state, and moves router/index/workspace readiness behind shared background warmup instead of relying on client patience during cold startup.

- Add additive `brain_init` in `src/brain-core/brain_mcp/_server_init.py` and `src/brain-core/brain_mcp/server.py`, with `warmup` and cheap `debug` flags plus coarse `readiness`, `warmup_state`, and `next_action` fields.
- Move router/index/workspace startup work behind shared background warmup in `src/brain-core/brain_mcp/server.py`, and make `brain_session`, `brain_read`, `brain_search`, `brain_list`, `brain_create`, `brain_edit`, `brain_move`, and readiness-dependent `brain_action` flows return structured progress/retry payloads while warmup is still running.
- Extend the shipped tool surface and profile defaults in `src/brain-core/defaults/config.yaml`, `src/brain-core/scripts/config.py`, `docs/functional/mcp-tools.md`, `docs/functional/config.md`, `docs/user/user-reference.md`, `src/brain-core/guide.md`, `docs/architecture/overview.md`, `docs/architecture/security.md`, `docs/architecture/decisions/dd-033-operator-profiles.md`, and `docs/architecture/decisions/dd-027-tool-resilience.md`.
- Cover the startup-boundary contract in `tests/test_mcp_server.py` and `tests/test_mcp_tool_contract.py`; `make test` now passes with `1884 passed, 4 skipped`.
