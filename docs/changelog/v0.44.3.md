# v0.44.3

**Move MCP transport ownership into _bootstrap/mcp_transport.py**

- Add `src/brain-core/scripts/_bootstrap/mcp_transport.py` as the shared launcher-safe owner for Claude/Codex MCP transport writes, SessionStart hook installation, recorded removal, and transport follow-up guidance.
- Add `src/brain-core/scripts/_bootstrap/vaults.py` and `src/brain-core/scripts/_bootstrap/workspace_scaffold.py` so public lifecycle wrappers stop reaching through `init.py` for vault discovery and Brain-local ignore-rule convergence.
- Collapse `src/brain-core/scripts/init.py` into the legacy compatibility CLI shell over the shared bootstrap owners, while `src/brain-core/scripts/setup.py`, `configure.py`, `repair.py`, and `_repair_runtime.py` call those owners directly.
- Update `src/brain-core/scripts/upgrade.py`, `src/brain-core/guide.md`, `docs/functional/scripts.md`, `docs/functional/mcp-tools.md`, `docs/architecture/overview.md`, and `src/brain-core/scripts/README.md` so user guidance points at `setup.py workspace` and `configure.py mcp` instead of treating `init.py` as the enduring implementation owner.
- Keep `cli/brain` dispatch behaviour unchanged; only `BRAIN_INSTALL_REF` moves to `v0.44.3` so the machine-global installer helper follows the shipped Brain version.
