# v0.68.3

**Summary:** Preserve MCP stdio across bounded proxy upgrades

**Type:** Additive transport lifecycle improvement

## Changed

- Add an explicit no-argument brain_proxy_restart control that replaces the selected installed proxy on POSIX while preserving stdio and starting fresh exceptional consent.
- Preflight public MCP session continuity, preserve bounded input read-ahead, quiesce output before draining, and refuse busy work without replaying commands.
- Bound teardown and consent cleanup, reap retired children, and correlate host replies with their originating child so failed or repeated handoffs cannot revive old consent or misroute responses.
