# v0.35.7 — 2026-05-07

**Add pid tags, latency, and gated body logging to MCP proxy**

A failed initialize handshake on a cold iCloud start emitted `child→client: id=82 method=None` in `mcp-proxy.log` with no way to tell whether the child returned a `result`, an `error`, or something malformed — the proxy logs only `id` and `method`, multiple concurrent proxies share the log file with no PID tag, and there is no per-message timing to attribute slow round-trips to the child versus the proxy. Added diagnostic improvements across `brain_mcp/proxy.py` and `brain_mcp/server.py`.

- Add `[pid=NNN]` to every log line and switch the formatter to default Python `asctime` for millisecond resolution in both proxy and server loggers, so concurrent processes' entries are demuxable across the shared log files and sub-second ordering is preserved.
- Add `BRAIN_LOG_BODIES=1` env var (off by default, read once at module import to match the file's existing env-var convention). When enabled, both `_reader_thread` and the main `run()` loop emit a follow-up DEBUG line with the full JSON body of every forwarded message — enough to reconstruct exactly what `id=82 method=None` actually contained on the next failure.
- Track per-message latency by storing `(request_obj, monotonic_send_time)` tuples in `_inflight_requests` (single source of truth, no parallel-dict drift risk). `_drain_inflight` unpacks via comprehension to keep its `list[dict]` return shape unchanged. Reader thread destructures on response and appends `latency=N.NNNs` to the existing `child→client` summary line.
