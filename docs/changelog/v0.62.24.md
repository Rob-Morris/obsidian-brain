# v0.62.24

**Summary:** Reject Windows local paths at the Git skill boundary

**Type:** Brain Core patch

## Changed

- Reject forward-slash and drive-relative Windows local paths before SCP-style remote parsing, keeping Git-backed skill acquisition remote-only on every supported platform.
