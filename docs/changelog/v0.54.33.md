# v0.54.33

**Migrate managed retrieval benchmark owners**

- Add managed operator commands for `retrieval.construct-benchmark` and
  read-only `retrieval.evaluate`.
- Keep both commands catalogue-visible but explicitly ineligible for MCP;
  CLI, direct script and Python remain supported projections.
- Restrict benchmark, audit and seed paths to the selected Brain and refuse
  writes into protected dot-prefixed system folders.
- Preserve explicit bucket targets, semantic mining strategy and mode selection
  without exposing adapter parser objects to application execution.
- Return structural planned/construction/evaluation results and receipt-backed
  fixture and audit effects; evaluation remains safely retryable and effect-free.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.33;
  CLI and proxy protocol versions remain unchanged.
