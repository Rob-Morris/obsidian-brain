# v0.68.8

**Summary:** Keep sandboxed commands observable and fallback creation coherent

**Type:** Backward-compatible patch; sandbox and fallback recovery

## Changed

- Keep successful read-only launcher commands independent of machine receipt storage while preserving durable effect-bearing outcomes.
- Emit validated, content-free one-shot failure records to stderr when the selected Brain diagnostic log is unwritable, retaining correlation IDs without exposing exception text or paths.
- Make compile_router.py help inert, accept an explicit vault, and republish router state after legacy fallback creates so batch creation remains coherent.
- Ignore only the recurring repo-root :memory:.ses machine-session marker without hiding other .ses files.
