# v0.36.8

Summary: Record vault-local upgrade stage progress before CLI follow-up work

- Teach `upgrade.py` to write in-progress stage snapshots to `.brain/local/last-upgrade.json` before long-running upgrade phases such as backup, copy, compile validation, migration replay, definition sync, semantic repair, and vault-local dependency sync.
- Preserve the existing final result log, but stop leaving operators with stale old success data when the caller loses stdout/stderr or a later follow-up step takes over the wall-clock time.
- Add regression coverage for both the pre-compile validation stage snapshot and the CLI dependency-sync handoff so the diagnostic file remains useful even when the final JSON never reaches the caller.
