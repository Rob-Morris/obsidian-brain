# v0.54.24

**Migrate atomic typed artefact-type definition owners**

- Add distinct operator commands for `type.create` and `type.replace` with
  explicit living/temporal classification and separate definition/template
  content.
- Accept each document through inline or staged content, reject reuse of one
  staged handle for both documents and consume both handles only after the
  bundle commits.
- Require independent optimistic SHA-256 preconditions for taxonomy and
  template replacement.
- Reuse the existing two-file `define.py` write/rollback authority while the
  application layer owns locking, authority, receipts, dry-run and uncertain
  outcome policy.
- Return typed bundle identity, vault-relative paths, frontmatter type, status
  enum and before/after hashes without caller-file or machine-local paths.
- Align Brain Core, the README badge and `BRAIN_INSTALL_REF` at v0.54.24;
  CLI and proxy protocol versions remain unchanged.
