# v0.68.5

**Summary:** Block artefact naming patterns from reaching protected Brain folders

**Type:** Patch; naming and write boundary integrity fix

## Changed

- Validate single-filename patterns in compile_router.py and resolve_naming_pattern, rejecting paths, separators and drive prefixes before definition writes or rendering.
- Check fully resolved content destinations through safe_write_artefact and safe_write_active_or_archived_artefact; make rename.py reject source scope changes and protected destinations before effects while preserving configuration access through existing operation owners.
- Add traversal, cached-pattern and symlink regressions across test_compile_router.py, test_definitions.py, test_create.py, test_common_filesystem.py, test_rename.py and tests/edit/, alongside legitimate naming patterns.
