# v0.53.1

**Normalize legacy shaping completion metadata before compilation**

- Prefix malformed `## Shaping` fields in `compile_router.py` with the stable `SHAPING_METADATA_INVALID` code so the upgrade path can distinguish repairable legacy metadata from unrelated compiler failures.
- Extend `migrate_to_0_53_0.py` to convert the previously shipped prose `Completion status` form only when it contains exactly one unambiguous backticked status, then apply the existing lifecycle post-condition checks.
- Exercise both the migration helper and a complete v0.52.1-to-v0.53.1 upgrade in `tests/test_upgrade.py`, including a taxonomy whose lifecycle already declares `open` and `shaping`.
- Align `src/brain-core/VERSION`, the README badge, `BRAIN_INSTALL_REF`, and template-vault tracking metadata with v0.53.1; `BRAIN_CLI_VERSION` remains unchanged because CLI dispatch behaviour did not change.
