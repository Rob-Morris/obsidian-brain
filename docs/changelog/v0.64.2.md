# v0.64.2

**Summary:** Preserve MCP results and invocation safety across client protocols

**Type:** Additive Brain Core patch

## Changed

- Return compact `brain.command-result/1` JSON as the first MCP text block labelled `audience: ["assistant"]`, with the existing concise one-liner labelled `audience: ["user"]`, from `brain_mcp/_result_content.py`.
- Keep `structuredContent` unchanged so hosts that already inject the envelope do not gain a third schema.
- Append proxy drift notes to the user text block in `_decorate_with_drift_note`, never the assistant JSON.
- Cover the projection in `tests/mcp_server/test_result_content.py`, `tests/mcp_server/test_granular_command_adapter.py`, and `tests/mcp_server/test_proxy_protocol_gate.py`. Require the Grok capture in `tests/capture_grok_mcp_client.py` to parse the model-visible tool text as the envelope, not the concise one-liner.

- Negotiate the same invocation contract for legacy initialization and modern per-request clients, including hosts that omit discovery. Re-establish it before replacement replay and receipt resolution.
- Require proxy protocol 3 and reject caller-owned invocation metadata. Older proxies receive restart guidance without corrupting the JSON fallback.
- Verify material reads, mutations, invocation rejection and real drift recovery through the production proxy in both protocol eras.
