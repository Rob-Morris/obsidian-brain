# v0.54.58

**Summary:** Define the strict MCP command-interface header contract

- Add the stdlib-only `brain_mcp._interface_protocol` owner for proxy protocol
  2 and `brain.command-interface-header/1` validation.
- Derive every MCP tool mapping through `application_interface_header()` from
  the authoritative application catalogue and mechanical projection grammar.
- Carry command identity/version, mutation class, catalogue/result schemas,
  interface epoch and catalogue fingerprint without a second semantic map.
- Give the header its own deterministic fingerprint and reject missing,
  unknown, malformed, contradictory or tampered facts before replay use.
- Extract `brainCommandInterface` only from the MCP initialise result's
  experimental capabilities extension and validate protocol compatibility.
- Keep the contract staged: proxy spawning, server emission and call gating
  follow in Phase 5 checkpoints before the coordinated public cutover.
